#summary model->get_instance() function
#labels Phase-Implementation

= Standard Arguments =
 * mixed $key


= Details =

Gets an instance of a row with key field: "$key"

the key_field is configurable using the static property $index_field_values


= Return =
returns an object of the called class, representing the row of the table.