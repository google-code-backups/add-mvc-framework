#summary RoadMap: Plans for new versions
#labels Featured,Phase-Design
= 0.8 =
 * controllers
   * hierachy system
 * class autoloading
   * class directory variable
   * iterate through class directory variable for autoloading
 * add-usability.js
 * model_ `*`::meta_columns()

= 0.9 =
 * sub_modes
 * common_gpcs
 * process_common_pre_mode

= 0.10 =
 * includes/views/pages/ - directory where all pages will be placed
 * includes/views/layout/ - directory where all layouts will be placed
 * ctrl_default_page - a ctrl for undeclared controllers with corresponding views/pages/

= 0.11 =
 * "add_current_user" & "current_user" class that will contain functions for the current user
   * ip_in_network()
   * is_developer()
   * geoip()
   * $track_pages = false;


= Version x.x = 
 * add class
   * Change add class to add_application.class.php so developers can extend it with add.class.php
 * models
   * model->< foreign_model_name >() by using __call and a static array 
   * Auto foreign key detection
   * Columns fetching and validation on valid_magic_property
   * block_rules - model support
 * autoloading classes
   * :: __loaded() pseudo-magic function
   * i_add_class interface