#summary ctrl_*->execute()
#labels Phase-Implementation,controllerfunction


= Description =

Execute is the main function of controllers, it is suppose to contain all the operations the controller is suppose to do.

For page controllers, it will contain the process_data() and the print_response() execution of the page. Meaning it'll control the overall process and response of the page.